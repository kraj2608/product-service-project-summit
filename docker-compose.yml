version: "3.1"

services:
  mongo:
    image: mongo
    restart: always
    command: "mongod --port 27018"
    ports:
      - "27018:27018"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: password

